/**
 * 프로덕션 빌드에서 MDEditor에서 import한 '@mdxeditor/editor/style.css'이 오류로 인해 로드되지 않는 문제를 해결하기 위한 컴포넌트
 * 차후 해결책을 찾으면 수정 필요
 */
import '@mdxeditor/editor/style.css';

const FixStyleLoad = () => null;

export default FixStyleLoad;
