import * as archive from './archive';
import * as auth from './auth';
import * as dashboard from './dashboard';
import * as utils from './utils';

const client = { auth, dashboard, archive, utils };

export default client;
